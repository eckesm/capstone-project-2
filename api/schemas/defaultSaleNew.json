{
    "$id": "http://example.com/example.json",
    "$schema": "http://json-schema.org/draft-07/schema",
    "default": {},
    "description": "The defaultSaleNew schema comprises the entire JSON document.",
    "examples": [
        {
            "restaurantId": 1,
            "mealPeriodId": 3,
            "dayId": 7,
            "total": 5000.0,
            "notes": "Expected Sunday dinner sales"
        }
    ],
    "required": [
        "restaurantId",
        "mealPeriodId",
        "dayId",
        "total"
    ],
    "title": "defaultSaleNew: schema for creating default sale record.",
    "type": "object",
    "properties": {
        "restaurantId": {
            "$id": "#/properties/restaurantId",
            "type": "integer",
            "title": "The restaurantId schema",
            "description": "An explanation about the purpose of this instance.",
            "default": 0,
            "examples": [
                1
            ]
        },
        "mealPeriodId": {
            "$id": "#/properties/mealPeriodId",
            "type": "integer",
            "title": "The mealPeriodId schema",
            "description": "An explanation about the purpose of this instance.",
            "default": 0,
            "examples": [
                3
            ]
        },
        "dayId": {
            "$id": "#/properties/dayId",
            "type": "integer",
            "title": "The dayId schema",
            "description": "An explanation about the purpose of this instance.",
            "default": 0,
            "examples": [
                7
            ]
        },
        "total": {
            "$id": "#/properties/total",
            "default": 0.0,
            "description": "An explanation about the purpose of this instance.",
            "examples": [
                5000.0
            ],
            "title": "The total schema",
            "maximum": 99999999,
            "minimum": -99999999,
            "type": "number"
        },
        "notes": {
            "$id": "#/properties/notes",
            "type": "string",
            "title": "The notes schema",
            "description": "An explanation about the purpose of this instance.",
            "default": "",
            "examples": [
                "Expected Sunday dinner sales"
            ]
        }
    },
    "additionalProperties": true
}