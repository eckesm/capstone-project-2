{
    "$id": "http://example.com/example.json",
    "$schema": "http://json-schema.org/draft-07/schema",
    "default": {},
    "description": "The mealPeriodNew schema comprises the entire JSON document.",
    "examples": [
        {
            "restaurantId": 1,
            "name": "Happy Hour",
            "notes": "Drink service only before full dinner service."
        }
    ],
    "required": [
        "restaurantId",
        "name"
    ],
    "title": "mealPeriodNew: schema for creating a new meal period.",
    "type": "object",
    "properties": {
        "restaurantId": {
            "$id": "#/properties/restaurantId",
            "type": "integer",
            "title": "The restaurantId schema",
            "description": "An explanation about the purpose of this instance.",
            "default": 0,
            "examples": [
                1
            ]
        },
        "name": {
            "$id": "#/properties/name",
            "default": "",
            "description": "An explanation about the purpose of this instance.",
            "examples": [
                "Happy Hour"
            ],
            "title": "The name schema",
            "maxLength": 50,
            "minLength": 1,
            "type": "string"
        },
        "notes": {
            "$id": "#/properties/notes",
            "type": "string",
            "title": "The notes schema",
            "description": "An explanation about the purpose of this instance.",
            "default": "",
            "examples": [
                "Drink service only before full dinner service."
            ]
        }
    },
    "additionalProperties": true
}