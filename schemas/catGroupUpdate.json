{
    "$id": "http://example.com/example.json",
    "$schema": "http://json-schema.org/draft-07/schema",
    "default": {},
    "description": "The updateCatGroup schema comprises the entire JSON document.",
    "examples": [
        {
            "name": "Retail",
            "notes": "Hats, shirts, bags, pins, etc."
        }
    ],
    "required": [
        "name"
    ],
    "title": "updateCatGroup: schema to update a category group.",
    "type": "object",
    "properties": {
        "name": {
            "$id": "#/properties/name",
            "type": "string",
            "title": "The name schema",
            "description": "An explanation about the purpose of this instance.",
            "default": "",
            "examples": [
                "Retail"
            ]
        },
        "notes": {
            "$id": "#/properties/notes",
            "type": "string",
            "title": "The notes schema",
            "description": "An explanation about the purpose of this instance.",
            "default": "",
            "examples": [
                "Hats, shirts, bags, pins, etc."
            ]
        }
    },
    "additionalProperties": true
}